import 'styled-components';

declare module 'styled-components' {
  export interface DefaultTheme {
    color: {
      vanilla: string;
      brown: string;
      black: string;
      lightGray: string;
      white: string;
      darkGray: string;
      darkPink: string;
      deepOrange: string;
      darkBlack: string;
    };
  }
}
